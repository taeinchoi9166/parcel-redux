import React, {Component} from 'react';
import './Header.scss';

class Header extends Component{
    render(){
        return(
          <header>
              <h1>Hellos.</h1>
          </header>
        );
    }
}

export default Header;